<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :user_id, :value => session[:user_id] %>
  
  <div class="field">
    <%= f.label :yesterday %><br>
    <%= f.text_area :yesterday %>
  </div>
  <div class="field">
    <%= f.label :today %><br>
    <%= f.text_area :today %>
  </div>
  <div class="field">
    <%= f.label :impediments %><br>
    <%= f.text_area :impediments %>
  </div>
  <div class="field">
    <%= f.label :task_date %><br>
    <%= f.date_select :task_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
